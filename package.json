{
  "name": "iot-monitoring-platform",
  "description": "Real-time IoT Industrial Monitoring Dashboard",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "frontend:install": "cd frontend && npm install",
    "frontend:dev": "cd frontend && npm run dev",
    "frontend:build": "cd frontend && npm run build",
    "backend:install": "cd backend && npm install",
    "backend:dev": "cd backend && npm run dev",
    "backend:start": "cd backend && npm start",
    "start:dev": "concurrently \"npm run backend:dev\" \"npm run frontend:dev\"",
    "setup": "npm run frontend:install && npm run backend:install",
    "install:all": "npm run frontend:install && npm run backend:install",
    "build": "npm run frontend:build"
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  },
  "workspaces": [
    "frontend",
    "backend"
  ]
}
